# Simple-CI

Простой набор sh-скриптов для развёртывания и поддержки сервисов.
Пайплайн: **git → test → beta → prod**.

## Быстрый старт

```bash
# 1. Настройка окружений и первичное развёртывание
bash utils/install.sh

# 2. Проверка статуса
bash utils/status.sh

# 3. Обновление теста из git
bash utils/git2test.sh

# 4. Продвижение test → beta
bash utils/test2beta.sh

# 5. Продвижение beta → prod
bash utils/beta2prod.sh
```

## Структура

```
├── project/           Исходный код проекта (шаблон)
│   └── manifest.json  Версия проекта
├── doc/               Документация
│   └── environment.md Описание переменных и окружений
├── utils/             Скрипты развёртывания
│   ├── common.sh      Общая библиотека
│   ├── install.sh     Интерактивная настройка
│   ├── git2test.sh    Обновление TEST из git
│   ├── test2beta.sh   Продвижение TEST → BETA
│   ├── beta2prod.sh   Продвижение BETA → PROD
│   └── status.sh      Статус версий
└── .env               Конфигурация (генерируется, не коммитится)
```

## Пайплайн

```
project/  ──git2test──▶  TEST  ──test2beta──▶  BETA  ──beta2prod──▶  PROD
```

- **TEST** — обновляется из git (вручную или автоматически)
- **BETA** — обновляется из TEST после ручной проверки
- **PROD** — обновляется из BETA с двойным подтверждением

## Скрипты

### `install.sh` — Настройка

Интерактивный мастер настройки. Запрашивает:
- Домены и папки для test / beta / prod
- Параметры БД (общая для beta+prod, отдельная для test)
- Настройки git (репозиторий, ветка)
- Опционально: автодеплой на тест (cron или git hook)

Проверяет готовность папок и выполняет первичное развёртывание.

### `git2test.sh` — Обновление TEST

Двухкомпонентный скрипт:
1. **Фаза 1:** скачивает обновления из указанной git-ветки
2. **Фаза 2:** обновляет TEST новейшей версией скриптов

Поддерживает автоматический режим (`--auto`) для cron/hooks.

### `test2beta.sh` — TEST → BETA

Копирует файлы из TEST в BETA, сохраняя конфиги и пользовательские данные.
Создаёт бэкап перед обновлением.

### `beta2prod.sh` — BETA → PROD

Копирует файлы из BETA в PROD. Требует двойное подтверждение
(yes + ввод слова "DEPLOY"). Создаёт бэкап.

### `status.sh` — Статус

Показывает таблицу версий всех окружений, сравнивает их и подсказывает
следующие действия.

## Требования

- `bash` 4.0+
- `rsync`
- `git`

## Документация

Подробное описание переменных, окружений и связей: [doc/environment.md](doc/environment.md)
